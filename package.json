{
  "name": "brils",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:tma": "npm run dev --workspace @brils/tma",
    "dev:bot": "npm run dev --workspace @brils/bot",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  }
}
