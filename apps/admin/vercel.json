{
  "buildCommand": "npm run build --workspace @brils/admin",
  "outputDirectory": "apps/admin/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
