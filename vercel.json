{
  "version": 2,
  "buildCommand": "npm run build --workspace @brils/tma && rm -rf dist && cp -R apps/tma/dist dist",
  "outputDirectory": "dist",
  "framework": null,
  "functions": {
    "apps/bot/api/*.ts": {
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/notify",
      "destination": "/apps/bot/api/notify"
    },
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
